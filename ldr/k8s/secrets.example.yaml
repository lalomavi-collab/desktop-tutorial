# Example only — DO NOT commit real secrets. Create via:
#   kubectl create secret generic ldr-secrets -n ldr \
#     --from-literal=peer-salt="$(openssl rand -hex 32)" \
#     --from-literal=postgres-password="$(openssl rand -hex 24)"
apiVersion: v1
kind: Secret
metadata:
  name: ldr-secrets
  namespace: ldr
type: Opaque
stringData:
  peer-salt: "replace-me-with-random-32-bytes"
  postgres-password: "replace-me-with-random-password"
