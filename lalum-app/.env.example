# Supabase connection for the LALUM client area.
# Copy to .env.local and fill in. When these are absent, the app runs in a
# self-contained demo mode: auth and portal forms work locally without network.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
