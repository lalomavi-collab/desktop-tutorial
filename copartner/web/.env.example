# Own Supabase project for CoPartner AI (separate from lalum-app and ldr/web).
# The publishable/anon key is safe to ship in the browser bundle; RLS on
# copartner_applications is what actually protects the data (see supabase/schema.sql).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
