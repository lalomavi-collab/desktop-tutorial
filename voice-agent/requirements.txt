# Voice AI Agent runtime dependencies.
# Pinned to lower bounds; resolve exact versions in your lockfile.
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
websockets>=13.0
httpx>=0.27.0
anthropic>=0.42.0
asyncpg>=0.29.0
pydantic>=2.7.0
python-dotenv>=1.0.0

# Dev / test
pytest>=8.0.0
pytest-asyncio>=0.23.0
